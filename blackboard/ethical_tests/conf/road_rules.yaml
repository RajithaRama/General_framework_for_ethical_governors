- [1, [Environment.vehicle_speed, Environment.speed_limit], Environment.vehicle_speed > Environment.speed_limit, False]
- [2, [Environment.no_of_pessengers, Environment.vehicle_speed, Environment.speed_limit], ( Environment.no_of_pessengers > 1 ) and ( Environment.vehicle_speed < Environment.speed_limit ), True]